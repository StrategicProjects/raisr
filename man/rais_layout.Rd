% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{rais_layout}
\alias{rais_layout}
\title{Record layout of the RAIS microdata}
\usage{
rais_layout(type = "vinculos")
}
\arguments{
\item{type}{\code{"vinculos"} (employment relationships, the default) or
\code{"estabelecimentos"} (establishments).}
}
\value{
A tibble with columns \code{column} (normalized name), \code{original}
(header of the \verb{;} files), \code{original_2023} (header of the \verb{,} files),
\code{type} and \code{description}.
}
\description{
The columns of the files, with the normalized name returned by
\code{\link[=rais_read]{rais_read()}}, the header used by the Ministry in the \verb{;} files (up to the
RAIS 2022, and the partial and legacy editions), the header used in the
\verb{,} files (RAIS 2023 onwards), the type assigned when \code{types = TRUE} and a
short description. The official layouts (one spreadsheet per period,
"RAIS_vinculos_layout*.xls" and "RAIS_estabelecimento_layout*.xls") are
published in the \code{Layouts} folder of the FTP server; the categories of
every code are listed there.
}
\details{
Files before the RAIS 2018 have fewer columns (for example the monthly
remuneration columns start in 2015 and are named \verb{vl_rem_<mes>_cc} up to
2017 and \verb{vl_rem_<mes>_sc} from 2018), and the oldest years use a
different classification of occupations (\code{cbo_ocupacao}) and education
(\code{grau_instrucao_2005_1985}). Two columns exist only from the RAIS 2023
onwards: \code{ind_vinculo_abandonado} and \code{categoria_trabalhador}.
}
\examples{
rais_layout()
rais_layout("estabelecimentos")
subset(rais_layout(), type == "double")$column
}
