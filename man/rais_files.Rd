% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/available.R
\name{rais_files}
\alias{rais_files}
\title{Archives needed for a year and a set of states (offline)}
\usage{
rais_files(year, uf = NULL, type = "vinculos", edition = "final")
}
\arguments{
\item{year}{Reference years (integer or character vector).}

\item{uf}{Optional states to cover, as IBGE two-digit codes (\code{26}) or
two-letter abbreviations (\code{"PE"}). \code{NULL} covers every state (all
regional files, or all 27 state files).}

\item{type}{Which files: \code{"vinculos"} (employment relationships, the
default), \code{"estabelecimentos"} (establishments) or both.}

\item{edition}{\code{"final"} (the default), \code{"parcial"} (the preliminary
edition some years receive before the final one) or \code{"legado"} (the
previous files, kept in a \code{Legado} sub-folder when a year is
re-published).}
}
\value{
A tibble with one row per archive and columns \code{year}, \code{edition},
\code{type}, \code{group}, \code{file} and \code{url}.
}
\description{
Builds, without touching the network, the list of archives that hold the
RAIS microdata of one or more years for the requested states. This is the
routing rule of the server:
}
\details{
\itemize{
\item from the RAIS \strong{2018} onwards, the employment relationships
(\code{vinculos}) are published in one file per group of states
(\code{RAIS_VINC_PUB_NORTE.7z}, \code{..._NORDESTE.7z}, \code{..._MG_ES_RJ.7z},
\code{..._SP.7z}, \code{..._SUL.7z}, \code{..._CENTRO_OESTE.7z}), plus a small
\code{RAIS_VINC_PUB_NI.7z} with records whose municipality is not
identified; the establishments are in a single national
\code{RAIS_ESTAB_PUB.7z};
\item up to the RAIS \strong{2017}, there is one file per state
(\code{PE2017.7z}, \code{SP2017.7z}, ...) and a national \code{ESTB2017.7z}.
}
}
\examples{
rais_files(2024, uf = "PE")
rais_files(2017, uf = c(26, 29), type = c("vinculos", "estabelecimentos"))
rais_files(2024)$file
}
