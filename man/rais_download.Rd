% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{rais_download}
\alias{rais_download}
\title{Download RAIS archives}
\usage{
rais_download(
  year,
  uf = NULL,
  type = "vinculos",
  edition = "final",
  cache_dir = NULL,
  force = FALSE,
  timeout = 3600,
  verbose = NULL
)
}
\arguments{
\item{year}{Reference years (integer or character vector).}

\item{uf}{Optional states to cover, as IBGE two-digit codes (\code{26}) or
two-letter abbreviations (\code{"PE"}). \code{NULL} covers every state (all
regional files, or all 27 state files).}

\item{type}{Which files: \code{"vinculos"} (employment relationships, the
default), \code{"estabelecimentos"} (establishments) or both.}

\item{edition}{\code{"final"} (the default), \code{"parcial"} (the preliminary
edition some years receive before the final one) or \code{"legado"} (the
previous files, kept in a \code{Legado} sub-folder when a year is
re-published).}

\item{cache_dir}{Optional cache directory (see \code{\link[=rais_cache_dir]{rais_cache_dir()}}).}

\item{force}{Re-download archives already in the cache?}

\item{timeout}{Timeout in seconds for each file.}

\item{verbose}{Emit progress messages? Defaults to
\code{getOption("raisr.verbose", TRUE)}.}
}
\value{
A tibble with one row per archive and columns \code{year}, \code{edition},
\code{type}, \code{group}, \code{file}, \code{path} (local path, \code{NA} when not available),
\code{status} (\code{"downloaded"}, \code{"cached"}, \code{"not_found"} or \code{"error"}) and
\code{url}.
}
\description{
Fetches from the PDET/MTE FTP server the \verb{.7z} archives that hold the
RAIS microdata of one or more years for the requested states, into the
local cache (see \code{\link[=rais_cache_dir]{rais_cache_dir()}}). The archives to fetch are chosen by
the routing rule described in \code{\link[=rais_files]{rais_files()}}: one regional file per
group of states from 2018 onwards, one file per state before that, and a
national establishments file in both cases. Archives already in the
cache are not downloaded again unless \code{force = TRUE}.
}
\details{
The files are large: a regional employment file has from about 130 MB
(North) to more than 1 GB (Sao Paulo) compressed, and the Ministry's
server is slow at times. The default timeout allows one hour per file.
}
\examples{
# Which archives would be fetched (offline):
rais_files(2024, uf = "PE", type = c("vinculos", "estabelecimentos"))
\dontrun{
# Pernambuco, RAIS 2024: the NORDESTE regional file (about 600 MB)
rais_download(2024, uf = "PE")
}
}
\seealso{
\code{\link[=rais_read]{rais_read()}} to read a downloaded archive, \code{\link[=rais_fetch]{rais_fetch()}} for
download and read in one call.
}
