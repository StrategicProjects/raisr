% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{rais_cache_dir}
\alias{rais_cache_dir}
\title{Cache directory used by raisr}
\usage{
rais_cache_dir(cache_dir = NULL)
}
\arguments{
\item{cache_dir}{Optional path. When given, it is returned as is (after
creating the folder).}
}
\value{
The cache directory path, created if needed.
}
\description{
Downloaded archives are stored in a local cache so that a file is never
downloaded twice. Inside the cache, archives keep their server names in
one sub-folder per year and edition (\code{2024/RAIS_VINC_PUB_NORDESTE.7z},
\code{2023-parcial/RAIS_ESTAB_PUB.7z}, \code{2017/PE2017.7z}). The location is
resolved in this order:
}
\details{
\enumerate{
\item the \code{cache_dir} argument;
\item the \code{RAISR_CACHE_DIR} environment variable;
\item the \code{raisr.cache_dir} R option;
\item a session-scoped folder under \code{\link[=tempdir]{tempdir()}}, which R removes when the
session ends.
}

Set one of the first three to keep the archives between sessions. The
regional files are large (from about 130 MB for the North region to
more than 1 GB for Sao Paulo) and change only when a year is
re-published, so a persistent cache is strongly recommended.
}
\examples{
rais_cache_dir()
\dontrun{
# Persistent cache for every session:
Sys.setenv(RAISR_CACHE_DIR = "~/dados/rais")
}
}
