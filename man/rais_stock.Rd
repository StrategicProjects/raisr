% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stock.R
\name{rais_stock}
\alias{rais_stock}
\title{Consolidate employment stock, admissions and separations}
\usage{
rais_stock(data, by = NULL)
}
\arguments{
\item{data}{A tibble returned by \code{\link[=rais_read]{rais_read()}} or \code{\link[=rais_fetch]{rais_fetch()}} for
\code{vinculos} files. Must contain \code{vinculo_ativo_31_12} and \code{rais_year};
the other columns are used when present.}

\item{by}{Character vector of additional grouping columns (for example
\code{"municipio"}, \code{"cnae_20_subclasse"}, \code{"sexo_trabalhador"}).}
}
\value{
A tibble with the grouping columns and \code{records} (records
aggregated), \code{stock} (relationships active on 31/12), \code{admissions}
and \code{separations} (when \code{mes_admissao} / \code{mes_desligamento} are
present), \code{december_payroll} (sum of \code{vl_remun_dezembro_nom} over the
active stock) and \code{mean_december_wage} (that sum divided by the stock
with a positive December wage), the last two when
\code{vl_remun_dezembro_nom} is present.
}
\description{
Aggregates employment records read with \code{\link[=rais_read]{rais_read()}} or \code{\link[=rais_fetch]{rais_fetch()}}
into the figures the Ministry publishes from the RAIS: the \strong{stock} of
employment relationships active on 31 December (\code{vinculo_ativo_31_12 == 1}), the admissions and separations that happened during the year
(records with a non-zero \code{mes_admissao} / \code{mes_desligamento}) and the
December payroll and mean wage of the active stock. The aggregation is
by \code{rais_year} plus any grouping columns you ask for.
}
\examples{
f <- system.file("extdata", "2024", "RAIS_VINC_PUB_NORDESTE_sample.7z", package = "raisr")
x <- rais_read(f, verbose = FALSE)
rais_stock(x, by = "municipio")
}
