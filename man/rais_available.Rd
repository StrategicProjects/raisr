% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/available.R
\name{rais_available}
\alias{rais_available}
\title{List the RAIS archives published on the PDET/MTE FTP server}
\usage{
rais_available(year = NULL, timeout = 30, verbose = NULL)
}
\arguments{
\item{year}{Optional integer vector of years to restrict the listing
(for example \code{2020:2025}). \code{NULL} (the default) lists every year since
1985, which takes one FTP request per year folder.}

\item{timeout}{Connection timeout in seconds for each FTP request.}

\item{verbose}{Emit progress messages? Defaults to
\code{getOption("raisr.verbose", TRUE)}.}
}
\value{
A tibble with one row per archive and columns \code{year}, \code{edition}
(\code{"final"}, \code{"parcial"} or \code{"legado"}), \code{type} (\code{"vinculos"} or
\code{"estabelecimentos"}), \code{group} (the region of a regional file, the
state of a pre-2018 file, or \code{NA}), \code{file}, \code{size_bytes}, \code{modified}
(\code{POSIXct}, server time) and \code{url}. Sorted from the most recent to the
oldest year. Returns an empty tibble, with a warning, when the server
cannot be reached.
}
\description{
Queries the public FTP server of the Ministry of Labour and Employment and
returns every RAIS archive currently published, one row per file, with
its size and the date it was last modified on the server. The Ministry
publishes the RAIS of a year around the second half of the following
year, sometimes preceded by a partial edition (\code{"parcial"}), and keeps the
previous files in a \code{Legado} folder when a year is re-published
(\code{"legado"}).
}
\examples{
\donttest{
# Requires network access to ftp.mtps.gov.br
files <- tryCatch(rais_available(year = 2024), error = function(e) NULL)
if (!is.null(files)) files[, c("edition", "type", "group", "size_bytes")]
}
}
\seealso{
\code{\link[=rais_files]{rais_files()}} to build the same table offline for a given
year and set of states, \code{\link[=rais_download]{rais_download()}} to fetch the archives.
}
